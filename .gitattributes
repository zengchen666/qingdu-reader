# ============================================================
# 换行与二进制文件的处理规则
#
# 为什么需要这个文件：这个项目在 Windows 上开发，但要让 Linux / macOS
# 上克隆下来也能正常工作，就必须明确"仓库里存什么、检出时转什么"。
# 不写的话，同一个文件在不同机器上会得到不同的字节，diff 里全是噪音。
# ============================================================

# 默认：文本按内容自动识别，仓库里统一存 LF
* text=auto eol=lf

# Windows 脚本和批处理在部分环境下对 LF 敏感，检出时保留 CRLF
*.ps1  text eol=crlf
*.bat  text eol=crlf
*.cmd  text eol=crlf

# ------------------------------------------------------------
# 以下是二进制：绝不做换行转换，也不做 diff
#
# samples/ 下的测试样本必须是"逐字节精确"的 ——
# 分章测试断言的是每个章节在文件里的**字节偏移区间**，
# 一旦 git 把 CRLF 换成 LF，偏移量就全变了，测试会莫名其妙地挂。
# 所以把它们整个标成 binary，让 git 完全不碰这些字节。
# ------------------------------------------------------------
samples/**  binary

*.png   binary
*.jpg   binary
*.jpeg  binary
*.gif   binary
*.ico   binary
*.icns  binary
*.ttf   binary
*.otf   binary
*.jar   binary
*.exe   binary
*.dll   binary
*.db    binary
*.sqlite binary

# ------------------------------------------------------------
# 语言统计
# 把文档和示例数据排除出"项目语言构成"，让主页正确显示以 Java 为主。
# 注意「不」排除 scripts/ —— 那些是自己写的构建脚本，
# 把它们藏起来只是让数字好看，不是事实。
# ------------------------------------------------------------
docs/**     linguist-documentation
samples/**  linguist-documentation
*.md        linguist-documentation
